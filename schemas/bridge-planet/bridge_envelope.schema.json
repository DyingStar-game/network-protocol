{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "BridgeEventEnvelope (planet)",
  "description": "Bridge envelope for the planet service. Extend with planet-specific event schemas as the protocol is defined.",
  "type": "object",
  "required": ["event_type", "name", "payload"],
  "properties": {
    "event_type": { "type": "string", "enum": ["core", "plugin"] },
    "namespace":  { "type": ["string", "null"] },
    "name":       { "type": "string" },
    "payload":    { "type": "object" }
  }
}
