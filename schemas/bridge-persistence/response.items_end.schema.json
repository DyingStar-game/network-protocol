{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "persistence response: items_end",
  "description": "Sent by persistence to Horizon after all items_chunk messages have been sent, signalling the end of the get_all_items response.",
  "type": "object",
  "required": ["event_type", "name", "payload"],
  "properties": {
    "event_type": { "type": "string", "enum": ["core", "plugin"] },
    "namespace":  { "type": ["string", "null"] },
    "name":       { "type": "string", "enum": ["items_end"] },
    "payload":    { "type": "object" }
  }
}
