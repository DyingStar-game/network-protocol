{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "persistence: update_item",
  "description": "Sent by Horizon bridge to persistence to update a full item record in the in-memory cache.",
  "type": "object",
  "required": ["event_type", "name", "payload"],
  "properties": {
    "event_type": { "type": "string", "enum": ["core", "plugin"] },
    "namespace":  { "type": ["string", "null"] },
    "name":       { "type": "string", "enum": ["update_item"] },
    "payload": {
      "$ref": "#/$defs/item"
    }
  },
  "$defs": {
    "vec3": {
      "type": "object",
      "required": ["x", "y", "z"],
      "properties": {
        "x": { "type": "number" },
        "y": { "type": "number" },
        "z": { "type": "number" }
      }
    },
    "item": {
      "type": "object",
      "required": ["object_type", "uuid", "object_data"],
      "properties": {
        "object_type": { "type": "string" },
        "uuid":        { "type": "string" },
        "parent_id":   { "type": ["string", "null"] },
        "scenename":   { "type": ["string", "null"] },
        "position":    { "$ref": "#/$defs/vec3" },
        "rotation":    { "$ref": "#/$defs/vec3" },
        "object_data": { "type": "array" }
      }
    }
  }
}
