{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "persistence: get_all_items",
  "description": "Sent by Horizon bridge to persistence to request all stored items. The service responds with one or more items_chunk messages followed by items_end.",
  "type": "object",
  "required": ["event_type", "name", "payload"],
  "properties": {
    "event_type": { "type": "string", "enum": ["core", "plugin"] },
    "namespace":  { "type": ["string", "null"] },
    "name":       { "type": "string", "enum": ["get_all_items"] },
    "payload":    { "type": "object" }
  }
}
