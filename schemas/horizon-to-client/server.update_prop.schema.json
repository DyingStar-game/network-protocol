{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "server.update_prop",
  "description": "Sent by Horizon to update an existing prop's state on the client.",
  "type": "object",
  "required": ["namespace", "event", "data"],
  "properties": {
    "namespace": { "type": "string", "enum": ["server"] },
    "event":     { "type": "string", "enum": ["update_prop"] },
    "data": {
      "type": "object",
      "required": ["object_type", "object_uuid", "object_data"],
      "properties": {
        "object_type": { "type": "string" },
        "object_uuid": { "type": "string" },
        "object_data": {
          "type": "object",
          "properties": {
            "position": { "$ref": "#/$defs/vec3" },
            "rotation": { "$ref": "#/$defs/vec3" }
          }
        }
      }
    }
  },
  "$defs": {
    "vec3": {
      "type": "object",
      "required": ["x", "y", "z"],
      "properties": {
        "x": { "type": "number" },
        "y": { "type": "number" },
        "z": { "type": "number" }
      }
    }
  }
}
