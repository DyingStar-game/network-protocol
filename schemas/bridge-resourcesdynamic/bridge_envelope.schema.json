{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "BridgeEventEnvelope (resourcesDynamic)",
  "description": "Shared bridge envelope used in both directions between Horizon and the resourcesDynamic service.",
  "type": "object",
  "required": ["event_type", "name", "payload"],
  "properties": {
    "event_type": { "type": "string", "enum": ["core", "plugin"] },
    "namespace":  { "type": ["string", "null"] },
    "name":       { "type": "string" },
    "payload":    { "type": "object" }
  }
}
